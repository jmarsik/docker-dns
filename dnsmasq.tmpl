# static config

auth-server=cats,docker0
domain=cats
expand-hosts
local-service
domain-needed
bogus-priv
no-resolv
server=8.8.8.8

# autogenerated hosts starts here
{{ range $host, $containers := groupByMulti $ "Env.DOMAIN_NAME" "," }}{{/*
    */}}{{ range $index, $container := $containers }}{{/*
        */}}{{ with $address := index $container.Addresses 0 }}
host-record=/{{ $host }}/{{ $address.IP }}
cname={{ $container.Name }},{{ $host }}
        {{ end }}{{/*
    */}}{{ end }}{{/*
*/}}{{ end }}
{{ range $hostname, $containers := groupBy $ "Name" }}
 # host container name = {{ $hostname }}
{{ end }}
# autogenerated hosts ends here